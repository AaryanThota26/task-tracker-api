apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  labels:
    {{- include "task-tracker.labels" . | nindent 4 }}
data:
  DB_HOST: postgres
  DB_PORT: "5432"
  DB_NAME: {{ .Values.postgres.database }}
  DB_USER: {{ .Values.postgres.user }}

  REDIS_HOST: redis
  REDIS_PORT: "6379"
