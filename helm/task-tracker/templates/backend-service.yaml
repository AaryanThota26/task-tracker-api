apiVersion: v1
kind: Service
metadata:
  name: {{ include "task-tracker.fullname" . }}-backend
  labels:
    app: {{ include "task-tracker.fullname" . }}-backend
    {{- include "task-tracker.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: {{ include "task-tracker.fullname" . }}-backend
  ports:
  - port: {{ .Values.backend.service.port }}
    targetPort: {{ .Values.backend.service.port }}
