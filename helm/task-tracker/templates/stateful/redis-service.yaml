apiVersion: v1
kind: Service
metadata:
  name: {{ include "task-tracker.fullname" . }}-redis
  labels:
    app: {{ include "task-tracker.fullname" . }}-redis
    {{- include "task-tracker.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: {{ include "task-tracker.fullname" . }}-redis
  ports:
  - port: {{ .Values.redis.service.port }}
    targetPort: {{ .Values.redis.service.port }}
