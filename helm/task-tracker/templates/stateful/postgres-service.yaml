apiVersion: v1
kind: Service
metadata:
  name: {{ include "task-tracker.fullname" . }}-postgres
  labels:
    app: {{ include "task-tracker.fullname" . }}-postgres
    {{- include "task-tracker.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: {{ include "task-tracker.fullname" . }}-postgres
  ports:
  - port: {{ .Values.postgres.service.port }}
    targetPort: {{ .Values.postgres.service.port }}
